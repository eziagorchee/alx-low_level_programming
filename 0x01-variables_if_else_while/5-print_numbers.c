#include <stdio.h>
/**
 * main - The entry point
 * Return: outputs int result
 */

int main(void)
{
	int start;

	start = 0;

	while (start < 10)
	{
		printf("%d", start);
		start++;
	}
	printf("\n");
	return (0);
}
