#include <stdio.h>
#include "main.h"
/**
 * main -The entry point of the program
 * Return: outputs result
 */

int main(void)
{
	char start = 'a';
	print_alphabet(void)
	{
		while (start <= 'z')
		{
			_putchar(start);
			start++;
		}
		_putchar('\n');
		return (void);
	}
	return (0);
}
